# Custom `$` operator for hdClass Returns a clean data.frame when using hd_obj\$matrix

Custom `$` operator for hdClass Returns a clean data.frame when using
hd_obj\$matrix

## Usage

``` r
# S3 method for class 'hdClass'
x$name
```

## Arguments

- x:

  An object of class 'hdClass'

- name:

  The field to extract
