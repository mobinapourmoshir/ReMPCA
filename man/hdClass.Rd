% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdClass.R
\name{hdClass}
\alias{hdClass}
\title{Hybrid Data Constructor (\code{hdClass})}
\usage{
hdClass(hdlist, argval = NULL, Smoothing_parameter = 0, Sparsity_parameter = 0)
}
\arguments{
\item{hdlist}{A list of \code{fdClass}, \code{rdClass}, or \code{imgClass} objects.}

\item{argval}{Optional numeric vector of grid points along the rows. If \code{NULL}, it defaults to a uniform grid over 0 and 1.}

\item{Smoothing_parameter}{Smoothing parameter(s) for the rows:
\itemize{
\item If 0, no smoothing is applied.
\item If a numeric value or vector, it is used directly.
\item If \code{NULL}, defaults to \code{2^seq(-30, 5, length.out = 10)} for tuning.
}}

\item{Sparsity_parameter}{Sparsity parameter(s) for the rows:
\itemize{
\item If 0, no sparsity is applied.
\item If a numeric vector, it is used for tuning.
\item If \code{NULL}, a suitable default sequence is generated.
}}
}
\value{
An object of class \code{hdClass} (a matrix) with several hybrid-aware attributes.
}
\description{
Constructs an object of class \code{hdClass}, representing hybrid data composed of multiple functional and/or raw variables.
}
\details{
The \code{hdClass} is an S3 class for representing \emph{hybrid data}, consisting of a list of functional (\code{fdClass}), raw (\code{rdClass}), or image (\code{imgClass}) data objects.
\itemize{
\item The input list (\code{hdlist}) can contain a mix of \code{fdClass}, \code{rdClass}, and \code{imgClass} objects.
\item All objects must have the \strong{same number of observations} (i.e., same number of rows).
\item Image objects (\code{imgClass}) are interpreted as either functional or raw based on their internal class inheritance.
}

The resulting object is a matrix with column-wise concatenation of the input matrices and includes the following attributes:
\itemize{
\item \code{GridPoints_u}: Row grid points.
\item \code{Smoothing_parameter}: Row smoothing parameter(s).
\item \code{Sparsity_parameter}: Row sparsity parameter(s).
\item \code{n_var}: Number of variables (i.e., number of objects in \code{hdlist}).
\item \code{ncol}: Number of columns in each variable.
\item \code{Smoothing_parameter_col}: List of smoothing parameters for each variable.
\item \code{Sparsity_parameter_col}: List of sparsity parameters for each variable.
\item \code{GridPoints_v}: List of grid points (columns) for each variable.
\item \code{variable_types}: Character vector of type labels for each variable: \code{"hd"} (functional) or \code{"rd"} (raw).
}
}
\examples{
fd_obj <- fdClass(matrix(rnorm(100), nrow = 10))
rd_obj <- rdClass(matrix(rnorm(100), nrow = 10))

hd_obj <- hdClass(
  hdlist = list(fd_obj, rd_obj),
  argval = seq(0, 1, length.out = 10),
  Smoothing_parameter = 0.5,
  Sparsity_parameter = 2
)

print(hd_obj)
attr(hd_obj, "variable_types")  # Shows "hd", "rd", etc.

}
